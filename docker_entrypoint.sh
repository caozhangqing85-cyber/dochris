#!/bin/bash
# ============================================================
# Dochris Docker 入口脚本
# ============================================================
# 默认行为：显示帮助信息
# 可通过 CMD 或 docker-compose command 覆盖

set -e

# 如果没有传入参数，显示帮助
if [ $# -eq 0 ]; then
    exec kb --help
else
    # 执行传入的命令
    exec "$@"
fi
